apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wimc-net.fullname" . }}-test-connection"
  labels:
    {{- include "wimc-net.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wimc-net.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
